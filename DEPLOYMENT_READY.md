# Deployment Ready

GitHub Pages deployment is configured through `.github/workflows/pages.yml` and targets the `main` branch.

This marker confirms the `main` branch is ready for the Pages deployment workflow.
